name: blood_bank_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=3.1.1 <4.0.0" # Specify a proper range for the Dart SDK

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  english_words: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/blood_logo.png # Ensure proper indentation for assets
